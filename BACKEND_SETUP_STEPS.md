# إعداد الباك إند — أسرة صناع الحياة بالجامعات المصرية

الخطوات دي بتكمّل دليل الديبلوي اللي اتكتب في المحادثة. الجزء ده خاص بالحاجات
اللي بتتظبط **جوه محرر Apps Script نفسه** (مش في كود `Code.gs`)، وهي 3 حاجات:
ملف الـ manifest (`appsscript.json`)، خدمة Drive المتقدمة، ومكتبة Firestore.

## 1) ملف appsscript.json (الـ manifest)

الملف ده بيحدد الصلاحيات (oauthScopes) اللي المشروع محتاجها عشان يقدر يكتب في
الشيت، يرفع على درايف، يبعت إيميلات، يعمل شهادات PDF، ويكلم خدمات خارجية زي
كولدينري وQR وفايرستور.

**إزاي تضيفه:**
1. من محرر Apps Script، اضغط على أيقونة الترس ⚙️ (Project Settings) على
   الشمال.
2. فعّل الخيار **"Show `appsscript.json` manifest file in editor"**.
3. هيظهر ليك ملف `appsscript.json` في قايمة الملفات على الشمال — افتحه،
   امسح اللي فيه، والصق مكانه محتوى ملف `appsscript.json` اللي بعتهولك.
4. احفظ (Ctrl+S).

لو الخطوة دي متعملتش، Apps Script بيحاول يحدد الصلاحيات تلقائي، وده بيسبب
مشاكل غريبة (زي رفض الاتصال بكولدينري أو فشل توليد الشهادات) لأن التحديد
التلقائي مابيغطيش كل الحالات.

## 2) خدمة Drive API المتقدمة (Advanced Drive Service)

مطلوبة لرفع وتحويل قوالب الشهادات (Google Docs → PDF) ومشاركة صور المسجلين.

**إزاي تفعّلها:**
1. في محرر Apps Script، من القايمة الجانبية اضغط **Services** (فيها علامة +).
2. دوّر على **Drive API** واضغط **Add**.
3. سيبها على أحدث نسخة (v2) واضغط Add تاني للتأكيد.

(ده منفصل عن `DriveApp` العادي اللي شغال من غير أي إعداد — الخدمة دي بتفتح
إمكانيات إضافية زي تحويل صيغ الملفات).

## 3) مكتبة Firestore (اختياري — لو عايز نسخة احتياطية لحظية في فايربيز)

الشيت هو المصدر الأساسي للبيانات دايمًا، وده اختياري بالكامل — لو معملتوش،
كل حاجة هتفضل شغالة عادي. لكن لو عايز كل تسجيل يتنسخ فورًا لـ Firestore
كمان (نسخة احتياطية أو عشان تستخدمها في تطبيق تاني):

**أ) إنشاء مشروع Firebase ومفتاح خدمة (Service Account JSON):**
1. روح [console.firebase.google.com](https://console.firebase.google.com)
   واعمل مشروع جديد (أو استخدم مشروع موجود بالفعل خاص بيكم).
2. فعّل **Firestore Database** من القايمة الجانبية.
3. روح لإعدادات المشروع ⚙️ ▸ **Service Accounts** ▸ **Generate new private key**.
4. هيتنزلك ملف JSON فيه 3 حاجات محتاجهم: `client_email`، `private_key`،
   و`project_id`.

**ب) إضافة مكتبة FirestoreApp لمشروع Apps Script:**
1. في محرر Apps Script، من القايمة الجانبية اضغط **Libraries** (فيها +).
2. في خانة "Script ID" الصق القيمة دي بالظبط:
   `1VUSl4b1r1eoNcRWotZM3e87ygkxvXltOgyDZhixqncz9lQ3MjfT1iKFw`
3. اضغط **Look up**، اختار أحدث نسخة، وسيب الاسم (Identifier) زي ما هو
   `FirestoreApp` (الكود بيستخدم الاسم ده بالظبط).
4. اضغط **Add**.

**ج) حفظ بيانات الاتصال في Script Properties:**
1. من محرر Apps Script ▸ ⚙️ Project Settings ▸ نزّل لتحت لـ
   **Script Properties**.
2. ضيف 3 خصائص (من ملف الـ JSON اللي نزل في الخطوة أ):
   - `FIRESTORE_PROJECT_ID` = قيمة `project_id`
   - `FIRESTORE_CLIENT_EMAIL` = قيمة `client_email`
   - `FIRESTORE_PRIVATE_KEY` = قيمة `private_key` (بالسطور الجديدة `\n` زي
     ما هي بالظبط جوه علامتي التنصيص)

**د) التأكد إنها شغالة:**
- افتح صفحة الداشبورد ▸ فيه زرار **"🩺 تشخيص"** بيعرض حالة `firestoreMirror` —
  لازم يظهر "شغالة ✓" بدل رسالة الخطأ.
- أو افتح مباشرة: `SCRIPT_URL?action=diagnostics&password=كلمة_السر_بتاعتك`

---

**ملحوظة**: كل الإعدادات دي (الـ manifest، Drive API، Firestore) خاصة
بمشروع Apps Script بتاعكم إنتوا بس — مفيهاش أي حاجة متشاركة مع مشروع سند.
